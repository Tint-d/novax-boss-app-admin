import { Modal } from "@mantine/core";

const Test = () => {
  return (
    <Modal opened={true} onClose={() => {}} title="Authentication" centered>
      {/* Modal content */}
    </Modal>
  );
};

export default Test;
